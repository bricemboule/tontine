# TontineOS — Dépendances Python backend
# Installer avec : pip install -r requirements.txt

# ── Core API ──────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── Base de données ───────────────────────────────────────────
sqlalchemy==2.0.35
asyncpg==0.29.0           # Driver PostgreSQL async
alembic==1.13.2           # Migrations

# ── Auth & Sécurité ───────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # Hash mots de passe
python-multipart==0.0.9            # Form data

# ── Validation ────────────────────────────────────────────────
pydantic[email]==2.8.2
pydantic-settings==2.4.0

# ── Cache & Queue ─────────────────────────────────────────────
redis==5.0.8
slowapi==0.1.9             # Rate limiting (anti brute-force login)
celery==5.4.0
flower==2.0.1              # Monitoring Celery

# ── HTTP Client (Mobile Money) ────────────────────────────────
httpx==0.27.2

# ── Notifications ─────────────────────────────────────────────
africastalking==1.2.8      # SMS Africa's Talking
aiosmtplib==3.0.1          # Email async

# ── Export ────────────────────────────────────────────────────
openpyxl==3.1.5            # Export Excel
weasyprint==62.3           # Export PDF

# ── Stockage ──────────────────────────────────────────────────
boto3==1.35.0              # S3 / MinIO (reçus PDF)

# ── Dev & Tests ───────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.24.0
python-dotenv==1.0.1
